<Window x:Class="ProductionLineMonitor.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="生产线设备监控 V1"
        Width="1280" Height="720"
        MinWidth="1100" MinHeight="640"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Background="{StaticResource WindowBackgroundBrush}"
        Closing="Window_Closing">

    <Grid MaxWidth="1600" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶栏：设备信息 + 操作按钮 -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
                CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding DeviceName}"
                               FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <Border Style="{StaticResource StatusBadgeStyle}" Margin="0,0,8,0">
                            <TextBlock Text="{Binding OnlineStatus}"
                                       Foreground="{StaticResource NormalBrush}" FontSize="12"/>
                        </Border>
                        <Border Style="{StaticResource StatusBadgeStyle}">
                            <TextBlock Text="{Binding OperatingStatus}"
                                       Foreground="{Binding OperatingState, Converter={StaticResource OperatingStateToBrushConverter}}"
                                       FontSize="12"/>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="▶ 启动" Style="{StaticResource PrimaryButtonStyle}"
                            Click="StartButton_Click" IsEnabled="{Binding CanStart}" Margin="0,0,8,0"/>
                    <Button Content="■ 停止" Style="{StaticResource StopButtonStyle}"
                            Click="StopButton_Click" IsEnabled="{Binding CanStop}" Margin="0,0,8,0"/>
                    <Button Content="↺ 复位" Style="{StaticResource ResetButtonStyle}"
                            Click="ResetButton_Click" IsEnabled="{Binding CanReset}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 指标区：四列卡片 -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Metrics}"
                      Margin="0,0,0,16">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource MetricCardBorderStyle}" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="13" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Value}"
                                           FontSize="32" FontWeight="Bold"
                                           Foreground="{Binding Level, Converter={StaticResource MetricLevelToBrushConverter}}"/>
                                <TextBlock Text="{Binding Unit}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="14" VerticalAlignment="Bottom"
                                           Margin="6,0,0,6"/>
                            </StackPanel>
                            <Border Style="{StaticResource StatusBadgeStyle}"
                                    Background="{Binding Level, Converter={StaticResource MetricLevelToBrushConverter}}"
                                    HorizontalAlignment="Left" Margin="0,8,0,0">
                                <TextBlock Text="{Binding StatusText}" Foreground="White" FontSize="12"/>
                            </Border>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 采样区：DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding RecentReadings}"
                  Style="{StaticResource MonitorDataGridStyle}"
                  Margin="0,0,0,16"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat=HH:mm:ss}" Width="100"/>
                <DataGridTextColumn Header="温度 °C" Binding="{Binding Temperature, StringFormat=F1}" Width="100"/>
                <DataGridTextColumn Header="压力 MPa" Binding="{Binding Pressure, StringFormat=F3}" Width="100"/>
                <DataGridTextColumn Header="转速 rpm" Binding="{Binding Speed, StringFormat=F1}" Width="100"/>
                <DataGridTextColumn Header="产量" Binding="{Binding Production}" Width="80"/>
                <DataGridTemplateColumn Header="状态" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="6,2"
                                    Background="{Binding OverallLevel, Converter={StaticResource MetricLevelToBrushConverter}}"
                                    HorizontalAlignment="Center">
                                <TextBlock Text="{Binding OverallLevel}" Foreground="White" FontSize="12"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 状态栏 -->
        <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}"
                CornerRadius="6" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding LastUpdatedText}"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                <TextBlock Grid.Column="1" Text="采样周期: 1 秒"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"
                           HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding Message}"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"
                           HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</Window>
